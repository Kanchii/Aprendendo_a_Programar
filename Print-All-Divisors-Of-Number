#include <bits/stdc++.h>

void printDivisors(int n){
  vector<int> vet;
  for(int i = 0; i < sqrt(n); i++){
    if(n % i == 0){
      if(n / i == i){
        cout << i << " ";
      else {
        cout << i << " ";
        vet.push_back(n / i);
      }
    }
  }
  for(int i = vet.size() - 1; i >= 0; i--){
    cout << vet[i] << " ";
  }
}

int main(){
  printDivisors(100);
  
  return 0;
}
